<div id="login">
  <% if user_signed_in? %>
  <p>Welcome <%= current_user.username %>
  <%= link_to 'Logged In [click to logout]', destroy_user_session_path, :method => :delete%></p>
<% else %>
  <p>
  <%= render 'devise/sessions/form' %>
  </p>
<% end %>
</div>