  <h1>Update Profile</h1>
  <%= form_for @user,  multiple: true do |f| %>
  <div class="field">
    <%= f.text_area :first_name, placeholder: "First Name" %>
  </div>
  <div class="field">
    <%= f.text_area :last_name, placeholder: "Last Name" %>
  </div>
   <% if !current_user.has_role?(:student) %>
  <div class="field">
    <%= f.text_area :email, placeholder: "Email" %>
  </div>
  <div class="field">
    <%= f.text_area :cont_area, placeholder: "Content Area" %>
  </div>
  <%end%>
    <span class="picture">
    <%= f.file_field :avatar %>
  </span>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
